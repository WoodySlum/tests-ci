name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Poetry
        run: pip install poetry

      - name: Install dependencies
        run: poetry install

      - name: Run Black
        run: poetry run black --check .

      - name: Run Pylint
        run: poetry run pylint src/ || true  # Ne bloque pas la CI en cas d'avertissements

      - name: Run tests
        run: poetry run pytest

      - name: Ensure Dockerfile exists
        run: |
          if [ ! -f Dockerfile ]; then
            echo "Dockerfile is missing!"
            exit 1
          fi

      - name: Build Docker image
        run: docker build -t ${{ secrets.REGISTRY_URL }}/hello-world-ci:latest .

      - name: Login to Docker registry
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login ${{ secrets.REGISTRY_URL }} -u "${{ secrets.REGISTRY_LOGIN }}" --password-stdin

      - name: Push Docker image
        run: docker push ${{ secrets.REGISTRY_URL }}/hello-world-ci:latest